# Proposed schema only. No parser or CLI exists yet.
version: 1
mode: enforce
network:
  default: deny
  allow:
    - id: model-api
      host: model.example
      port: 443
      protocol: https
    - id: retrieval-api
      host: retrieval.example
      port: 443
      protocol: https
report:
  directory: ./runs
  formats: [json, html]
